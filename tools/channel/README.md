# channel — completeness and volume of the gmr channel vs enumeration

Ran 2026-09-05; results and verdicts are recorded in
`tools/experiments-2026-09.md` — **read that before re-running anything here**.
Pre-registered criteria live in `facts.py`, byte accounting in `meter.py`,
arms in `run.sh` (a1 gmr-only · a2 enumeration-only · a3 both · a1p src-less
corpus probe). `results-2026-09.jsonl` is the committed matrix.
